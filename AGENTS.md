# 开发约定

## 语言与范围

- 默认使用中文交流；界面、文档、必要注释、错误提示和 Git 提交说明优先中文。
- 保留常用英文代码标识符、命令和协议字段，不为中文化修改第三方接口。
- 先完成最小可用相册；人脸检索需要样本验证，不能把讨论中的候选功能写成已实现功能。
- 不主动引入账号、数据库、复杂任务平台或额外治理系统。

## 文件与协作

- 修改前检查 Git 状态和相关差异，保留其他人的未提交工作。
- 按明确文件清单暂存，不使用 `git add .`。
- 原片只读；衍生图和索引输出到独立目录，不能覆盖原片。
- 照片、人物特征、真实地址、凭据、日志和运行时数据禁止提交。文档只使用占位符。

## 验证与交付

- 验证真实入口与真实文件输入，覆盖 JPG/NEF 配对歧义、缺失文件和无效输入。
- 以手机浏览与下载体验为首要标准，控制首页请求量，避免在线转换原片。
- 部署使用独立目录与站点配置；先备份、检查 Nginx 配置，再 reload，并复查现有站点。
- HTTPS 检查必须校验证书链、有效期和地址匹配，不能用跳过校验的结果宣称正常。
- 区分本机验证与独立外网验证、定时器启用与实际运行成功。
- 报告已完成内容、验证结果和剩余限制；不得把计划描述为交付。
